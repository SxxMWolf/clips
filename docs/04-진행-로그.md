# 4. 진행 로그 (선택)

커밋 기간은 2025-12-31 ~ 2026-02-03. 메시지가 전부 `.`라서 나중에 다시 보면 뭐 했는지 파일 diff로만 짐작하게 된다. 습관이 별로였다.

### 2025-W52 / 2026-W01 (12.31 ~ 1.4)

병합, AI 프롬프트, AI 클리핑(YouTube·STT·클립)을 한꺼번에 올렸다. 폴더는 `merge/`, `prompt/`, `ai_clipping/`으로 나눴다.

이 무렵에 인물 크롭이 안 맞아서 비율이 이상했고, 프롬프트가 일정하지 않아 영상이 안 이어졌다. 후자는 공통 프롬프트로 맞춤.

### 2026-W02 ~ W04 (1.5 ~ 1월 중후반)

1.5~1.8에 병합 UI·프롬프트 손본 커밋이 있고, 그다음 주는 커밋이 없다. 따로 적어 둔 메모는 없음.

### 2026-W05 (1.31)

`ai_clipping/`이랑 AI 클립 페이지, `start_servers.sh`를 지웠다. Docker랑 `go`를 넣고, 남는 기능은 ASMR 프롬프트·병합 쪽으로 좁혔다. 왜 클리핑을 버렸는지는 이 로그에 안 적음.

### 2026-W06 (2.2 ~ 2.3)

letterbox 옵션이랑 병합·프롬프트 자잘한 수정. 세부 이유는 기억 안 남.
